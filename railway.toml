[build]
builder = "DOCKERFILE"
dockerfilePath = "cxr-backend/Dockerfile"
watchPatterns = ["cxr-backend/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
